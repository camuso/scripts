function ui_replaceline {

	local lc=$(cat $3 | wc -l)
	[ $1 > $lc ] && return 1

	awk -v line=$1 -v new_content="$2" '{
		if (NR == line) {
			print new_content;
		} else {
			print $0
		}
	}' $3
}

